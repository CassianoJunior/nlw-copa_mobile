import { NavigationContainer } from '@react-navigation/native';
import { SignIn } from '../screens/SignIn';

const Routes = () => {
  return (
    <NavigationContainer>
      <SignIn />
    </NavigationContainer>
  );
};

export { Routes };
